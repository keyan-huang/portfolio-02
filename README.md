# Performance Discussion Form Dashboard

Static portfolio website with the latest high-resolution PNG images and updated hero.

## Files
- `index.html`: page content and layout
- `style.css`: typography, colors, spacing, and responsive styles
- `pdf-image02/`: all 16 high-resolution images used in 18 placements

## Preview and edit
Open `index.html` in a browser. Edit content in `index.html` and styling in `style.css`. Keep the image folder beside these files. Google Fonts loads online, with local fallback fonts. No build or package installation is required.

## Add to GitHub
Extract the ZIP. Create a repository on GitHub, then upload the contents of this folder, including the entire `pdf-image02` folder. `index.html` and `style.css` should be at the repository root. Upload the extracted files, not the ZIP. Commit the upload.

The separate Figma interactive prototype remains in Figma; this export contains the website and its images.
